/**
 * 
 * scan icon => scan ticket => qr code validation
 * upcoming Events => press {event details}
 * FloatingActionButton Add Event
 * profile, settings
 * event history {all event that had been ended}
 */